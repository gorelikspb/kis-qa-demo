# План развертывания Mini-KIS QA Demo на GitHub

## ✅ Что уже сделано

### 1. Структура проекта
- ✅ Flask веб-приложение (`app.py`)
- ✅ HTML шаблоны (`templates/`)
- ✅ Автоматизированные тесты Playwright (`tests/`)
- ✅ Мануальные тест-кейсы (`manual_tests.md`)
- ✅ Bug-репорты (`BUGS.md`)
- ✅ Документация (README.md, DEPLOY.md)
- ✅ Зависимости (`requirements.txt`)
- ✅ Git ignore (`.gitignore`)

### 2. Функциональность
- ✅ Patient anlegen с валидацией
- ✅ Termin erfassen с валидацией
- ✅ Списки пациентов и терминов
- ✅ Обработка ошибок и успешных операций

### 3. Тестирование
- ✅ 18 мануальных тест-кейсов
- ✅ 20 автоматизированных GUI-тестов
- ✅ 3 примера bug-репортов

## 📋 Пошаговый план развертывания

### Шаг 1: Подготовка репозитория (ЛОКАЛЬНО)

#### 1.1. Проверка что внутренние документы не попадут в git

**Важно:** Убедитесь, что следующие папки/файлы НЕ попадут в репозиторий:
- `../cv_common/` - внутренние правила CV-проектов (не должна быть в репозитории)
- `../../common/` - общие инструкции (не должна быть в репозитории)
- Любые ссылки на локальные пути в документации

**Автоматическая проверка:**
```powershell
# В папке autotest
cd D:\dev\cv\autotest

# Запустите скрипт проверки
.\check-before-push.ps1
```

**Ручная проверка:**
```powershell
# Проверьте что нет ссылок на локальные пути
Select-String -Path "*.md" -Pattern "D:\\dev|cv_common|\.\.\/common" -Recurse

# Проверьте что будет добавлено в git
git status
git diff --cached --name-only
```

#### 1.2. Обновление README.md

Убедитесь, что в README.md нет ссылок на локальные пути. Если есть ссылка на `../README.md` (cv_common), её нужно удалить или заменить на описание.

#### 1.3. Инициализация Git (если еще не сделано)

```powershell
cd D:\dev\cv\autotest
git init
```

#### 1.4. Проверка .gitignore

Убедитесь, что `.gitignore` содержит все необходимое:
- `venv/`, `__pycache__/`, `.pytest_cache/`
- `playwright-report/`, `test-results/`
- `.env`, `.env.local`
- Логи и временные файлы

### Шаг 2: Создание GitHub репозитория

#### 2.1. Создайте репозиторий на GitHub

1. Зайдите на https://github.com/new
2. Название: `mini-kis-qa-demo` (или другое подходящее)
3. Описание: "QA Demo für vereinfachtes Klinik-Informationssystem"
4. **Public** (чтобы работодатель мог посмотреть)
5. НЕ добавляйте README, .gitignore, license (у нас уже есть)

#### 2.2. Подключите локальный репозиторий

```powershell
cd D:\dev\cv\autotest

# Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/mini-kis-qa-demo.git

# Проверьте
git remote -v
```

### Шаг 3: Первый коммит и push

#### 3.1. Добавьте файлы

```powershell
# ВАЖНО: Сначала запустите проверку!
.\check-before-push.ps1

# Проверьте что будет добавлено (dry-run)
git add -n .

# Если все правильно, добавьте файлы
git add .

# Проверьте статус
git status
```

**Убедитесь, что НЕ добавляются:**
- `venv/`
- `__pycache__/`
- `playwright-report/`
- `test-results/`
- Любые файлы из `../cv_common/` или `../../common/` (они не должны быть в папке проекта)

**Файлы которые ДОЛЖНЫ быть в репозитории:**
- ✅ `README.md`
- ✅ `DEPLOY.md`
- ✅ `DEPLOYMENT_PLAN.md` (план развертывания)
- ✅ `EMPLOYER_GUIDE.md` (руководство для работодателя)
- ✅ `QUICK_START.md` (быстрый старт)
- ✅ Все файлы проекта и тесты

#### 3.2. Создайте коммит

```powershell
git commit -m "Initial commit: Mini-KIS QA Demo

- Flask web application with Patient and Termin management
- 18 manual test cases
- 20 automated Playwright GUI tests
- Bug reports documentation
- Complete project documentation"
```

#### 3.3. Push на GitHub

```powershell
git branch -M main
git push -u origin main
```

### Шаг 4: Настройка репозитория для работодателя

#### 4.1. Добавьте описание репозитория

На странице репозитория на GitHub:
- Settings → General → Description
- Добавьте: "QA Demo für vereinfachtes Klinik-Informationssystem mit Testplanung, manuellen Tests, GUI-Testautomatisierung und Bug-Dokumentation"

#### 4.2. Добавьте темы (Topics)

На странице репозитория:
- Нажмите на шестеренку рядом с "About"
- Добавьте темы: `qa`, `testing`, `playwright`, `flask`, `python`, `test-automation`, `bug-reports`

#### 4.3. Обновите README с ссылкой на Live Demo

После развертывания Live Demo (см. Шаг 5), обновите README.md:
```markdown
## Live Demo

**Live Demo:** https://your-app-url.com
```

И сделайте коммит:
```powershell
git add README.md
git commit -m "Add live demo URL"
git push
```

### Шаг 5: Развертывание Live Demo

#### Вариант A: Railway (рекомендуется)

**Почему Railway:**
- ✅ Не засыпает (нет spin-down) - приложение всегда готово
- ✅ Быстрый отклик для демо-проектов
- ✅ Не нужен внешний пинг-сервис
- ✅ Лучше для показа работодателю (нет задержки первого запроса)
- ✅ Бесплатный план: $5 кредитов/месяц (обычно хватает для демо)

**Примечание:** Для онлайн развертывания виртуальное окружение создается автоматически на сервере. Не важно где оно было локально (`dev` или `dev_res`) - это не влияет на развертывание.

1. Зайдите на https://railway.app
2. Sign up / Sign in с GitHub
3. New Project → Deploy from GitHub repo
4. Выберите репозиторий `mini-kis-qa-demo`
5. Railway автоматически определит Python
6. Настройте переменные окружения (если нужно)
7. Deploy

**После развертывания:**
- Обновите README.md с URL
- Убедитесь, что приложение работает

#### Вариант B: Render (альтернатива)

⚠️ **Важно:** Render засыпает после 15 минут бездействия. Для постоянной доступности нужен пинг-сервис (UptimeRobot). См. инструкции ниже.

1. Зайдите на https://render.com
2. Sign up / Sign in с GitHub
3. New → Web Service
4. Connect repository: выберите `mini-kis-qa-demo`
5. Настройки:
   - **Name:** `mini-kis-qa-demo`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python app.py`
   - **Plan:** Free
6. Advanced → Add Environment Variable (если нужно):
   - `PYTHON_VERSION=3.11`
7. Create Web Service

**Настройка UptimeRobot (чтобы приложение не засыпало):**
1. Зайдите на https://uptimerobot.com
2. Sign up (бесплатно)
3. Add New Monitor
4. Monitor Type: HTTP(s)
5. Friendly Name: Mini-KIS QA Demo
6. URL: ваш Render URL
7. Monitoring Interval: 10 minutes
8. Create Monitor

**После развертывания:**
- Обновите README.md с URL
- Убедитесь, что приложение работает
- Проверьте что UptimeRobot пингует сайт

#### Вариант C: Heroku (устаревший, не рекомендуется)

```powershell
# Установите Heroku CLI
# Создайте Procfile
echo "web: python app.py" > Procfile

# Добавьте в requirements.txt (если нет)
echo "gunicorn>=21.2.0" >> requirements.txt

# Heroku deployment
heroku create mini-kis-qa-demo
git push heroku main
```

### Шаг 6: Финальная проверка

#### 6.1. Проверьте что работодатель видит:

✅ **На GitHub:**
- README.md с описанием проекта
- Структура проекта понятна
- Тесты видны в папке `tests/`
- Мануальные тесты в `manual_tests.md`
- Bug-репорты в `BUGS.md`
- Код читаемый и структурированный

✅ **Live Demo:**
- Приложение запускается
- Можно создать Patient
- Можно создать Termin
- Валидации работают
- Ошибки отображаются корректно

✅ **Тесты:**
- Можно запустить `pytest tests/ -v`
- Все тесты проходят (или видно какие не проходят)

## 🔍 Как работодатель будет смотреть проект

### 1. Первое впечатление (2-3 минуты)

**GitHub репозиторий:**
1. Откроет README.md
2. Увидит описание проекта
3. Проверит структуру файлов
4. Посмотрит на код (app.py, tests/)

**Что он увидит:**
- ✅ Четкое описание проекта
- ✅ Технологический стек
- ✅ Структура проекта
- ✅ Примеры тестов
- ✅ Bug-репорты

### 2. Live Demo (3-5 минут)

**Откроет Live Demo:**
1. Попробует создать Patient с валидацией
2. Попробует создать Termin
3. Проверит обработку ошибок
4. Увидит списки пациентов/терминов

**Что он оценит:**
- ✅ Работающее приложение
- ✅ UI выглядит профессионально
- ✅ Валидации работают корректно
- ✅ Ошибки понятные

### 3. Тестирование (5-10 минут)

**Мануальные тесты:**
- Откроет `manual_tests.md`
- Увидит 18 тест-кейсов
- Проверит структуру и детализацию

**Автоматизированные тесты:**
- Посмотрит `tests/test_patient.py`
- Посмотрит `tests/test_termin.py`
- Увидит использование Playwright
- Может запустить тесты локально

**Bug-репорты:**
- Откроет `BUGS.md`
- Увидит примеры документирования багов
- Оценит структуру и детализацию

### 4. Код (5-10 минут)

**Проверка качества кода:**
- Структура проекта
- Документирование функций
- Обработка ошибок
- Читаемость кода

## 📝 Чеклист перед показом работодателю

- [ ] Репозиторий на GitHub создан и публичен
- [ ] README.md содержит описание проекта
- [ ] Live Demo развернуто и работает
- [ ] Ссылка на Live Demo в README.md
- [ ] Все тесты проходят локально
- [ ] Код читаемый и структурированный
- [ ] Нет ссылок на локальные пути в документации
- [ ] .gitignore настроен правильно
- [ ] Репозиторий содержит только нужные файлы
- [ ] Описание репозитория на GitHub заполнено
- [ ] Темы (topics) добавлены

## 🚨 Важно помнить

1. **Внутренние документы остаются локально:**
   - `cv_common/` - НЕ в git
   - `dev/common/` - НЕ в git
   - Любые локальные инструкции - НЕ в git

2. **Публичный репозиторий содержит только:**
   - Код проекта
   - Документацию проекта
   - Тесты
   - Конфигурационные файлы проекта

3. **Перед каждым push проверяйте:**
   ```powershell
   git status
   git diff --cached
   ```

## 📞 Следующие шаги

После развертывания:
1. Протестируйте Live Demo самостоятельно
2. Проверьте что все ссылки работают
3. Убедитесь что тесты можно запустить
4. Подготовьте краткое описание проекта для сопроводительного письма

---

**Готово к развертыванию!** 🚀

